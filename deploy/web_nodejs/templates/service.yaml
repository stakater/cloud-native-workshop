apiVersion: v1
kind: Service
metadata:
  name: {{ include "web_nodejs.fullname" . }}
  labels:
    {{- include "web_nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web_nodejs.selectorLabels" . | nindent 4 }}
