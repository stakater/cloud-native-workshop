apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web_nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "web_nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web_nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
