##################################
# Application / App / Microservice
##################################
application:

  applicationName: "web"

  deployment:
    enabled: false

  configMap:
    enabled: false

  service:
    ports:
      - port: 8080
        name: https
        targetPort: 8080

  rbac:
    serviceAccount:
      enabled: true

  route:
    enabled: false


